node ('maven') {
    
      stage ('Code from SCM') {
        git credentialsId: 'github-demo-1', url: 'your github url'
      }
      stage ('Build') {
        sh '''mvn clean install'''
      }
      stage ('deploy to docker container')
    {
        sh '''docker build -t app:1 .
        docker run -d -p 8080:8080 --name web_app app:1'''
    }
}
